+ cleanup debug messages

+ introduce app

+ reintroduce settings

+ remember settings like `today` does

+ test extensively

+ compare sounds loudness with `relax`

+ prepare itch.io page, publish after playdate is delivered in america
